# todo-list — agent context

Working notes for automated runs (`app-update`). Facts only. If you had to
discover something the hard way, append it to **Gotchas** before you finish.

**What it is:** This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

**Stack:** Next.js, React, Tailwind, TypeScript

**Commands:**
- repo root: `npm run dev`, `npm run build`, `npm run start`, `npm run lint`

**Test setup:** vitest, @playwright/test

**Layout (dirs + key files):** `public/`, `src/`, `package.json`

**Deploy:** no `deploy.md` — `app-update` will fail this app until one exists.

## Gotchas

_Nothing recorded yet. Append what bites you._
